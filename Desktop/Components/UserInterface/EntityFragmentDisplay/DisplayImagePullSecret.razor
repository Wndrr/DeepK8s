@using k8s.Models
ImagePullSecrets:
@if (ImagePullSecrets == null)
{
    <NullValue/>
}
else
{
    @foreach (var imagePullSecret in ImagePullSecrets ?? new List<V1LocalObjectReference>())
    {
        <code>@imagePullSecret.Name</code>
    }
}

@code {

    [Parameter]
    public IList<V1LocalObjectReference>? ImagePullSecrets { get; set; }

}